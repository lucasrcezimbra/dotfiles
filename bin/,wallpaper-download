#!/bin/bash
set -e
set -u

NUMBER=$(python -c 'import random; print(random.randint(1, 52410))')
IMG_URL=$(curl -s "https://peapix.com/bing/$NUMBER" | pup 'div.gallery-item attr{data-src}')
curl "$IMG_URL" -o "$HOME/Pictures/wallpapers/$NUMBER.jpg"
