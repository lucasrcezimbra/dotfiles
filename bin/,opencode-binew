#!/bin/bash
set -e
set -u
set -o pipefail

if [[ $# -lt 1 ]]; then
  echo "Usage: ,opencode-binew <script-name> [description...]"
  exit 1
fi

opencode "$HOME/.dotfiles/" --prompt "/dotfiles-binew $*"
